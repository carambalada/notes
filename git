Make a repository:
 https://github.com/carambalada/tar-bar.git

Web:
 make a branch
 edit some files
 commit
 pull request
 pull request again
 merge with the master
 
 Console:
mkdir tar-bar
cd tar-bar
git init
#edit a_file
git add a_file
git commit -m "comment"
git remote add origin https://github.com/carambalada/tar-bar.git
git push -u origin master
